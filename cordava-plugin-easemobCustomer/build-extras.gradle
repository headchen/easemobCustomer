android {
    defaultConfig {
        minSdkVersion 16
        targetSdkVersion 22
    }

    ext.cdvMinSdkVersion = 20

    packagingOptions {
        exclude 'META-INF/NOTICE'
        exclude 'META-INF/LICENSE'
    }
}